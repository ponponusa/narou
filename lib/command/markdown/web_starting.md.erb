# 🚀 WEBサーバーを起動します

サーバーを起動しています。しばらくお待ちください。

<% if defined?(frontend_enabled) && frontend_enabled %>
- バックエンドAPI: http://<%= host %>:<%= port %>
- Web UI (開発用): http://<%= host %>:4321
<% else %>
- Web UI: http://<%= host %>:<%= port %>
<% end %>

処理を中止するには Ctrl+C を押してください。
