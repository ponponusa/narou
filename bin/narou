#! /usr/bin/env ruby
# frozen_string_literal: true

# 互換用エントリポイント。narou-mod コマンドへリダイレクトする。

warn "narou コマンドは廃止予定です。narou-mod を使用してください。" if $stderr.tty?

begin
  exec(File.expand_path("narou-mod", __dir__), *ARGV)
rescue SystemCallError
  load File.expand_path("narou-mod", __dir__)
end
